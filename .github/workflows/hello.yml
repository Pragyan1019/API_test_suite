name: Demo workflow

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code     
        uses: actions/checkout@v4    
      - name: Print hello
        run: echo 'Hello, GitHub Actions'

      - name: Show date
        run: date